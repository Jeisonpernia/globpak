<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="globpak.list">
      <field name="name">globpak list</field>
      <field name="model">globpak.globpak</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="globpak.action_window">
      <field name="name">globpak window</field>
      <field name="res_model">globpak.globpak</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="globpak.action_server">
      <field name="name">globpak server</field>
      <field name="model_id" ref="model_globpak_globpak"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="globpak" id="globpak.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="globpak.menu_1" parent="globpak.menu_root"/>
    <menuitem name="Menu 2" id="globpak.menu_2" parent="globpak.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="globpak.menu_1_list" parent="globpak.menu_1"
              action="globpak.action_window"/>
    <menuitem name="Server to list" id="globpak" parent="globpak.menu_2"
              action="globpak.action_server"/>
    -->
  </data>
</odoo>